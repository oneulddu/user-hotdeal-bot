# 성능 최적화 및 리팩토링 검토

2026-09-05, 기준 커밋 `28a913d`. DB/API, 크롤링·알림 처리, 세션 수명, 운영 스크립트와 CI 구성을 검토했다.
재현한 병목과 알림 보존 문제를 수정했으며 API 응답 형식, DB 스키마, 의존성, 크롤링 주기는 유지한다.

## 확인한 개선

| 경로 / 조건 | 변경 전 | 변경 후 / 검증 |
| --- | --- | --- |
| 캐시가 없는 피드 생성 | 게시글 조회와 사용하지 않는 전체 건수 조회, SQL 2회 | 건수 조회 없는 SQL 1회. RSS/Atom 모두 삭제·종료·사이트·크롤러 필터 및 최신 글 선택 확인 |
| 동일 피드 20개 동시 요청 | 조회·XML 생성 20회 | 조회·생성 1회. 다른 조건은 병렬 처리, 예외·취소 후 재시도 및 잠금 정리 확인 |
| SQLite 변수 한도 999, 게시글 100행 저장 | `too many SQL variables` 실패 | 50행 단위 저장. 1,051행을 22개 INSERT로 처리하고 1,050건 삭제는 3개 UPDATE로 처리 |
| 여러 SQL 배치의 트랜잭션 | 단일 대형 SQL | 같은 세션·트랜잭션을 사용하며 후속 배치 실패 시 앞선 배치도 롤백. 기존 ULID·생성 시각과 동일 갱신 시각 보존 |
| 폐기한 DB 엔진 | 팩터리 캐시의 강한 역참조로 GC 후에도 유지 | 팩터리가 살아 있는 동안만 캐시하여 엔진 해제 확인. 기본 세션은 공용 엔진 재사용 |
| 재로드 중 진행·대기 알림 | 덤프 과정에서 실행 큐를 비움 | 비파괴 스냅샷으로 보존. 반복 재로드와 저장 실패 후 `send → edit`가 한 번씩 처리됨 |
| 취소된 진행 알림의 복귀 | 전체 큐를 꺼내고 다시 넣음, O(n) | deque 선두 복귀 O(1). 큐의 완료 건수와 순서 보존 |
| 종료와 크롤링 동시 실행 | 진행 작업보다 세션 종료와 덤프가 먼저 실행될 수 있음 | 주기 완료 후 자원 종료·덤프. 최종 게시글·tombstone·미처리 알림 저장 확인 |
| 아카라이브 V2 첫 병렬 요청 | 브라우저 초기화가 중복 실행될 수 있음 | 20개 요청에서 시작 1회·종료 1회. 초기화 실패·취소 후 정리·재시도 확인 |
| 아카라이브 V15 반복 요청 | 요청마다 curl 세션 생성·종료 | 실제 localhost HTTP 요청 3회에서 TCP 연결 1개 재사용. 설정 쿠키만 전송하고 응답 쿠키는 다음 요청에 승계하지 않음 |

목록 API의 `total` 계산은 유지한다. RSS/Atom은 형식·크롤러·사이트·limit을 구분하는 최대 128개 캐시와 60초 TTL을 유지하며,
캐시 적중 시에도 인증 및 호출 제한을 적용한다. 캐시와 중복 생성 방지는 API 프로세스별로 동작한다.
동시 호출 수와 SQL 수는 회귀 테스트로 검증한 결과이며, 운영 환경의 응답시간 향상률을 의미하지 않는다.

## 운영 및 되돌리기

- 스키마 마이그레이션이나 설정 파일 변환이 필요 없다. 기존 `dump.json`을 그대로 읽는다.
- Compose 예제는 크롤러의 종료 유예시간을 2분으로 설정한다. 큰 타임아웃이나 여러 Scrapling URL을 사용하면 최장 주기와 저장 시간을 고려해 늘려야 한다.
- 코드 롤백 시 기존 이미지/커밋을 사용할 수 있다. 단, 롤백하면 이번에 수정한 큐 유실과 세션 수명 문제가 다시 발생할 수 있다.
- 실제 운영 DB나 Telegram 전송에는 접근하지 않았다. 운영 배포는 이 작업에 포함하지 않는다.

## 검증

Python 3.13.12, 저장소 `uv.lock` 기준:

```bash
uv sync --frozen
uv run --frozen ruff check src tests
uv run --frozen ruff format --check src tests
uv run --frozen pytest -q
```

전체 테스트 118개 통과, 외부 커뮤니티 연동 테스트 10개 제외. Ruff 검사와 포맷 검사 통과.
DB/API 변경은 `tests/test_repository_performance.py`, `tests/test_api_feed.py`, `tests/test_db_session.py`에서,
알림 보존과 자원 수명은 `tests/test_lifecycle.py`, `tests/test_crawler_sessions.py` 및 기존 회귀 테스트에서 확인한다.
MySQL은 SQL 방언 컴파일로 배치와 충돌 갱신 구문을 검증했으며 실제 MariaDB 서버 실행은 검증하지 않았다.
Scrapling 브라우저 초기화는 테스트 대역으로 확인했고 외부 사이트 접근 및 실제 브라우저 기동은 검증하지 않았다.

Opus 계획 검토는 연결 인증 오류로 실행되지 않았다. 직접 계획을 점검한 뒤 구현했으며 독립 Astra 코드 리뷰 결과는 PR에 기록한다.
